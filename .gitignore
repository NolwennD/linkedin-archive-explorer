out/
out-test/
dist/
build/
# Keep the data/ directory in the repo (so the default archive lookup has somewhere to
# look) but never commit the archives dropped into it.
data/*
!data/.gitkeep
test-data/anonymized/
*.class

# IntelliJ: config is not versioned directly. The template lives in ide/; run
# ide/setup-intellij.sh (or .cmd) to generate .idea/ and the root .iml.
.idea/
/linkedin-archive-explorer.iml

# Harnais de performance JMH : entièrement hors dépôt (jars, sources, résultats).
# La recette pour le remonter après un clone est dans
# docs/superpowers/specs/2026-07-28-jmh-benchmarks-design.md (le contexte) et
# docs/superpowers/plans/2026-07-28-jmh-benchmarks.md (la source, seule copie versionnée
# de bench et PipelineBenchmark.java).
benchmark/
